[cidlookup]
; https://crm.amocrm.com/private/acceptors/asterisk_new/?number=<NUMBER>&USER_LOGIN=<EMAIL>&USER_HASH=<HASH>
exten => cidlookup_1,1,Set(CRM(url)=evendabiz.amocrm.com)
same => n,Set(CRM(email)=olga.corlateanu.evenda@gmail.com)
same => n,Set(CRM(hash)=d7063a1067031296e7fc231b48a9ea81)
same => n,Set(CALLERID(name)=${SHELL(wget -O - --quiet https://${CRM(url)}/private/acceptors/asterisk_new/?number=${CALLERID(num)}\&USER_LOGIN=${CRM(email)}\&USER_HASH=${CRM(hash)}})
same => n,Return()